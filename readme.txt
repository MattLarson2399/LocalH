These are some functions that can be used to compute and study local h vectors using Polymake. The main file is a Perl script that can be loaded into polymake. 

This was primarily written over the summer of 2019 while working on a project with Sam Payne related to local h vectors. 